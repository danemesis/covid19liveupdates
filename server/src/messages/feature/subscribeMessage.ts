export const subscriptionResultMessage = (message): string => {
    return `You subscribed on ${message}`;
};

export const showMySubscriptionMessage = (message): string => {
    return `Here the list of your subscriptions ${message}`
};

export const subscribeCountryMessage = (message): string => {
    return `Test dan, ${message}`;
};
