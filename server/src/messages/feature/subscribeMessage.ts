export const subscribeError = (message: string): string => {
    return `${message}`
};

export const subscriptionResultMessage = (message: string): string => {
    return `You subscribed on ${message}`;
};

export const showMySubscriptionMessage = (message: string): string => {
    return `Here the list of your subscriptions ${message}`
};

export const subscribeCountryMessage = (message: string): string => {
    return `Test dan, ${message}`;
};
